<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >	
	<mapper namespace="Premium.A03_repository.dolistDao">
		<resultMap type="dolist" id="dolistList"/>

		<select id="my_do_List" resultMap="dolistList" parameterType="dolistsch">
			SELECT * FROM dolist
		</select>
		
	</mapper>